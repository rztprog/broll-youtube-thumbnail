'use strict';

// In Progress

/*
let startTimer = null;

const start = () => {
    const downloadButton = document.querySelector('.text-primary-foreground');
    const inputVideoUrl = document.querySelector('input[name="videoUrl"]');

    if (downloadButton && inputVideoUrl.value != "https://www.youtube.com/watch?v=XEO3duW1A80") {
        document.querySelector('.text-primary-foreground').click();
        clearInterval(startTimer);
    }
}

startTimer = setInterval(start, 50);
*/